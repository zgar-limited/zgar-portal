# 仪表盘和订单管理模块 - 硬编码中文扫描报告

## 执行摘要

| 指标 | 数值 |
|------|------|
| 扫描目录 | 2 个 (`components/dashboard`, `app/[locale]/(layout)/(dashboard)`) |
| 扫描文件数 | **22 个** |
| 硬编码中文项 | **233 项** |
| 高密度文件 | **2 个** (超过20个硬编码的文件) |

## 高密度文件列表（优先处理）

| 文件路径 | 硬编码数量 | 密度等级 |
|---------|----------|---------|
| `app/[locale]/(layout)/(dashboard)/account-addresses/AddressForm.tsx` | 33 项 | 🔴 高 |
| `components/dashboard/OrderDetails.tsx` | 32 项 | 🔴 高 |

## 扫描范围详情

### 组件层 (`components/dashboard/`) - 17 个文件

| 文件 | 硬编码数 | 主要硬编码类型 |
|------|---------|--------------|
| `AccountSettings.tsx` | 14 项 | 表单标签、占位符 |
| `AccountSummary.tsx` | 6 项 | 用户信息标签 |
| `ClosingInfoModal.tsx` | 14 项 | 模态框文本、错误提示 |
| `MyAccount.tsx` | 10 项 | 用户界面标签 |
| `OffcanvasSidebar.tsx` | 13 项 | 导航菜单项 |
| `OrderCard.tsx` | 2 项 | 按钮文字 |
| `OrderDetails.tsx` | 32 项 | 订单状态、追踪信息、标签 |
| `Orders.tsx` | 17 项 | 订单列表、状态标签 |
| `Sidebar.tsx` | 13 项 | 导航菜单项 |
| `TabsNavigation.tsx` | 9 项 | 标签页标题 |
| `Tasks.tsx` | 24 项 | 任务状态、奖励提示 |
| `payments/CreatePaymentModal.tsx` | 22 项 | 支付表单、验证提示 |
| `payments/PaymentRecordsList.tsx` | 18 项 | 支付记录列表 |
| `payments/PaymentSummaryCard.tsx` | 12 项 | 支付统计标签 |
| `payments/VoucherUploadArea.tsx` | 9 项 | 上传提示 |
| `transactions/BalanceTransactionTable.tsx` | 8 项 | 交易记录表头 |
| `transactions/PointsTransactionTable.tsx` | 8 项 | 积分交易表头 |

### 页面层 (`app/[locale]/(layout)/(dashboard)/`) - 5 个文件

| 文件 | 硬编码数 | 主要硬编码类型 |
|------|---------|--------------|
| `account-addresses/AddressForm.tsx` | 33 项 | 地址表单标签、国家列表 |
| `account-addresses/AddressesClient.tsx` | 12 项 | 地址管理界面 |
| `account-addresses/page.tsx` | 2 项 | 页面元数据 |
| `account-setting/page.jsx` | 3 项 | 页面标题 |
| `account-transactions/page.tsx` | 2 项 | 页面标题 |

## 分类统计

### 按功能模块分类

| 模块 | 硬编码数 | 占比 |
|------|---------|------|
| 订单管理 | 64 项 | 27.5% |
| 支付管理 | 61 项 | 26.2% |
| 地址管理 | 47 项 | 20.2% |
| 账户设置 | 20 项 | 8.6% |
| 交易记录 | 16 项 | 6.9% |
| 任务系统 | 24 项 | 10.3% |
| 导航菜单 | 13 项 | 5.6% |

### 按文本类型分类

| 文本类型 | 硬编码数 | 示例 |
|---------|---------|------|
| 表单标签 | 78 项 | "名字", "姓氏", "详细地址" |
| 按钮文字 | 35 项 | "保存", "取消", "确认" |
| 状态标签 | 28 项 | "已完成", "进行中", "已取消" |
| 占位符 | 42 项 | "请输入..." |
| 提示信息 | 32 项 | "保存失败，请重试" |
| 页面标题 | 18 项 | "账户设置", "订单详情" |

## 关键硬编码示例

### 1. 订单状态术语（需保持一致性）

```typescript
// components/dashboard/OrderDetails.tsx
"已完成" → OrderDetails.completed
"进行中" → OrderDetails.inProgress
"已取消" → OrderDetails.canceled
"审核中" → OrderDetails.underReview
"已退货" → OrderDetails.returned
```

### 2. 支付方式术语

```typescript
// components/dashboard/Orders.tsx
"余额支付" → Orders.balancePayment
"积分支付" → Orders.pointsPayment
"账期积分" → Orders.creditPoints
"手动支付" → Orders.manualPayment
"银行转账" → Orders.bankTransfer
```

### 3. 表单验证提示

```typescript
// app/[locale]/(layout)/(dashboard)/account-addresses/AddressForm.tsx
"请输入名字" → AddressForm.pleaseEnterFirstName
"请输入姓氏" → AddressForm.pleaseEnterLastName
"请输入详细地址" → AddressForm.pleaseEnterAddress
"请选择国家/地区" → AddressForm.pleaseSelectCountry
```

### 4. 国家/地区名称（AddressForm.tsx）

```typescript
"中国" → AddressForm.country.CN
"美国" → AddressForm.country.US
"加拿大" → AddressForm.country.CA
"英国" → AddressForm.country.GB
"澳大利亚" → AddressForm.country.AU
"日本" → AddressForm.country.JP
"韩国" → AddressForm.country.KR
"新加坡" → AddressForm.country.SG
"香港" → AddressForm.country.HK
"台湾" → AddressForm.country.TW
```

## 语义化翻译键命名规范

### 通用规范

```typescript
// 格式: {ComponentName}.{semanticKey}
MyAccount.balance
MyAccount.points
Orders.viewDetails
AddressForm.addAddress
```

### 特殊规范

1. **订单状态**: 使用统一的 `OrderStatus.*` 命名空间
2. **支付方式**: 使用统一的 `PaymentMethods.*` 命名空间
3. **国家名称**: 使用 `AddressForm.country.{ countryCode }` 格式
4. **验证提示**: 使用 `{Component}.validate.{field}` 格式

## 修复建议

### 优先级 1 - 高密度文件（立即处理）

1. **AddressForm.tsx** (33 项)
   - 国家名称列表需要国际化
   - 表单标签和验证提示
   - 建议创建 `AddressForm` 命名空间

2. **OrderDetails.tsx** (32 项)
   - 订单状态追踪文本
   - 支付和发货状态标签
   - 建议复用 `Orders` 和 `OrderStatus` 命名空间

### 优先级 2 - 常用术语（保持一致性）

确保以下术语在所有组件中使用相同的翻译键:

- 订单状态: `completed`, `inProgress`, `canceled`, `pending`
- 支付方式: `balancePayment`, `pointsPayment`, `bankTransfer`
- 通用操作: `save`, `cancel`, `confirm`, `delete`, `edit`

### 优先级 3 - 错误提示和验证消息

创建统一的验证消息命名空间:

```typescript
// messages/zh-CN.json
{
  "Validation": {
    "required": "请输入{field}",
    "invalid": "{field}格式不正确",
    "minLength": "{field}至少需要{min}个字符"
  }
}
```

## 实施步骤

### 步骤 1: 准备翻译文件

```bash
# 创建或更新 messages 文件
messages/
├── zh-CN.json
├── zh-TW.json
└── en-US.json
```

### 步骤 2: 添加翻译键

按照语义化命名规范添加所有硬编码文本的翻译。

### 步骤 3: 替换硬编码

```typescript
// 之前
<h1>账户设置</h1>

// 之后
import { useTranslations } from 'next-intl';
const t = useTranslations('AccountSettings');
<h1>{t('accountSettings')}</h1>
```

### 步骤 4: 测试验证

1. 检查所有页面的中英文切换
2. 验证表单验证提示的翻译
3. 测试订单状态显示
4. 检查动态内容的翻译（如模板字符串）

## 注意事项

### ⚠️ 特殊处理场景

1. **动态模板字符串**:
```typescript
// 需要使用翻译参数
`签到成功！获得 ${points} 积分`
→ t('checkInSuccess', { points })
```

2. **条件渲染的文本**:
```typescript
// 需要为每个变体提供翻译
isUploaded ? "已上传" : "未上传"
→ isUploaded ? t('uploaded') : t('notUploaded')
```

3. **复数形式**:
```typescript
`等 ${count} 件商品`
→ t('itemsCount', { count })
// 在 messages 中使用复数处理
```

## 后续工作

- [ ] 创建 `Dashboard` 命名空间翻译文件
- [ ] 创建 `Orders` 命名空间翻译文件
- [ ] 创建 `Payments` 命名空间翻译文件
- [ ] 创建 `AddressForm` 命名空间翻译文件（包含国家列表）
- [ ] 创建 `Tasks` 命名空间翻译文件
- [ ] 替换所有高密度文件的硬编码
- [ ] 测试语言切换功能
- [ ] 验证响应式布局在不同语言下的表现

## 相关文件

- 完整扫描报告: `dashboard-hardcoded-report.md`
- 扫描脚本: `scan-hardcoded-chinese-v2.js`
- 翻译文件目录: `messages/`

---

**扫描时间**: 2026-02-08
**扫描工具**: 自定义硬编码中文扫描脚本
**扫描范围**: 仪表盘和订单管理模块
